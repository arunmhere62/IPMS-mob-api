server {
    listen 80;
    server_name admin-api.indianpgmanagement.com;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    server_name admin-api.indianpgmanagement.com;

    ssl_certificate /etc/letsencrypt/live/admin-api.indianpgmanagement.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/admin-api.indianpgmanagement.com/privkey.pem;

    location / {
        resolver 127.0.0.11 ipv6=off valid=10s;
        set $admin_backend ipgm-admin-api;
        proxy_pass http://$admin_backend:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
